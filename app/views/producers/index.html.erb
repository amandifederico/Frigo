<p id="notice"><%= notice %></p>

<h1>Producers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Up date</th>
      <th>Down date</th>
      <th>City</th>
      <th>Observation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @producers.each do |producer| %>
      <tr>
        <td><%= producer.name %></td>
        <td><%= producer.up_date %></td>
        <td><%= producer.down_date %></td>
        <td><%= producer.city %></td>
        <td><%= producer.observation %></td>
        <td><%= link_to 'Show', producer %></td>
        <td><%= link_to 'Edit', edit_producer_path(producer) %></td>
        <td><%= link_to 'Destroy', producer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Producer', new_producer_path %>
