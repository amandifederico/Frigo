<p id="notice"><%= notice %></p>

<h1>Sub Productos</h1>

<table>
  <thead>
    <tr>
      <th>Lote</th>
      <th>Observacion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sub_products.each do |sub_product| %>
      <tr>
        <td><%= sub_product.lot.dte %></td>
        <td><%= sub_product.observartion %></td>
        <td>
          <%= link_to (fa_icon "eye"), sub_product, class: 'btn btn-sm btn-success' %>
          <%= link_to (fa_icon "pencil-square-o"), edit_sub_product_path(sub_product), class: 'btn btn-sm btn-warning' %>
          <%= link_to (fa_icon "trash-o"), sub_product, method: :delete, data: { confirm: '¿Seguro desea eliminar este registro?' }, class: 'btn btn-sm btn-danger' %>
              
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sub Product', new_sub_product_path %>
