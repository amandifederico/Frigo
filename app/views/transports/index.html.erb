<p id="notice"><%= notice %></p>

<h1>Transportes</h1>

<table>
  <thead>
    <tr>
      <th>Acoplado</th>
      <th>Capacidad</th>
      <th>Patente</th>
      <th>Modelo</th>
      <th>Chofer</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @transports.each do |transport| %>
      <tr>
        <td><%= transport.coupled %></td>
        <td><%= transport.capacity %></td>
        <td><%= transport.plate %></td>
        <td><%= transport.model %></td>
        <td><%= transport.driver %></td>
        <td>
          <%= link_to (fa_icon "eye"), transport, class: 'btn btn-sm btn-success' %>
          <%= link_to (fa_icon "pencil-square-o"), edit_transport_path(transport), class: 'btn btn-sm btn-warning' %>
          <%= link_to (fa_icon "trash-o"), transport, method: :delete, data: { confirm: '¿Seguro desea eliminar este registro?' }, class: 'btn btn-sm btn-danger' %>
              
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Transport', new_transport_path %>
